# Gitleaks configuration for Agent Relay MCP
# Excludes known non-secret patterns and false positives from the codebase.

title = "Agent Relay MCP gitleaks config"

[allowlist]
  description = "Global allowlist"
  paths = [
    '''uv\.lock''',
    '''\.gitleaks\.toml''',
  ]
